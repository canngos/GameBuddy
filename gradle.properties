org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=768m

# Gradle needs a JDK 17+ VM to start. JAVA_HOME should point at the JDK 25 LTS
# install; if a stale Java 8 shim ever wins on PATH, uncomment and adjust the line
# below (machine-specific — keep it out of commits).
# org.gradle.java.home=C:\\Program Files\\Java\\jdk-25.0.4
