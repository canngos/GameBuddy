# GENERATED by qa/perf/plan.js — do not edit.
#
# CX23 — 2 vCPU Intel, 4 GB
# SUT cores: 0,1   generator cores: 8-15
#
# memswap_limit equals mem_limit on purpose: without it Docker allows an equal amount of
# swap on top, so a container "limited" to 2 GB quietly uses 4 and the memory answer is
# wrong in the one direction that matters.

name: gamebuddy-perf

services:
  postgres:
    cpuset: "0,1"
    mem_limit: 900m
    memswap_limit: 900m
  backend:
    cpuset: "0,1"
    mem_limit: 2g
    memswap_limit: 2g
  model:
    cpuset: "0,1"
    mem_limit: 900m
    memswap_limit: 900m
  redis:
    cpuset: "0,1"
    mem_limit: 128m
    memswap_limit: 128m
