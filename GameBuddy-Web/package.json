{
  "name": "gamebuddy-web",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "npm run generate && astro dev",
    "build": "npm run generate && npm run check:legal && astro build",
    "preview": "astro preview",
    "generate": "node scripts/generate-tokens-css.mjs && node scripts/generate-og.mjs",
    "check:legal": "node scripts/check-legal.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource/chakra-petch": "^5.3.0",
    "@fontsource/poppins": "^5.3.0",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.2.2",
    "tailwindcss": "^4.3.3"
  }
}
